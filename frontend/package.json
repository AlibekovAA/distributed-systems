{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Frontend for e-commerce website",
  "scripts": {
    "start": "lite-server",
    "build": "tsc && npm run copy-files",
    "watch": "tsc -w",
    "copy-files": "find src -type f -not -name \"*.ts\" -exec cp --parents {} dist/ \\;"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "lite-server": "^2.6.1",
    "http-proxy-middleware": "^2.0.6"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
