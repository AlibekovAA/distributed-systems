{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Frontend for e-commerce website",
  "scripts": {
    "build": "tsc && npm run copy-files",
    "copy-files": "bash -c 'cp src/favicon.ico src/index.html dist/ && cp -r src/{styles,modules,pages,services,utils} dist/'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
