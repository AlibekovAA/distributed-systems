[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v -ra -q
markers =
    products: tests for products
    orders: tests for orders
    users: tests for users
    integration: integration tests
    cleanup: tests with data cleanup
